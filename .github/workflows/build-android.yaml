name: Build Android

on:
  workflow_call:
    inputs:
      version:
        required: true
        type: string
      flutter_version:
        required: true
        type: string
      build_number:
        required: false
        type: string
    secrets:
      KEYSTORE_BASE64:
        required: true
      KEYSTORE_PASSWORD:
        required: true
      KEY_PASSWORD:
        required: true
      KEY_ALIAS:
        required: true

jobs:
  build-android:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: app

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          flutter-version: ${{ inputs.flutter_version }}

      - name: Get Dependencies
        run: |
          flutter --version
          flutter pub get

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'
          cache: 'gradle'
          check-latest: true

      - name: Setup Android signing
        run: |
          echo "${{ secrets.KEYSTORE_BASE64 }}" | base64 --decode > android/app/keystore.jks
          echo "storePassword=${{ secrets.KEYSTORE_PASSWORD }}" > android/key.properties
          echo "keyPassword=${{ secrets.KEY_PASSWORD }}" >> android/key.properties
          echo "keyAlias=${{ secrets.KEY_ALIAS }}" >> android/key.properties
          echo "storeFile=keystore.jks" >> android/key.properties

      - name: Build for Android
        run: |
          flutter gen-l10n
          dart run build_runner build --delete-conflicting-outputs
          BUILD_NUM_FLAG=""
          if [ -n "${{ inputs.build_number }}" ]; then
            BUILD_NUM_FLAG="--build-number=${{ inputs.build_number }}"
          fi
          flutter build apk --release $BUILD_NUM_FLAG
          flutter build apk --split-per-abi $BUILD_NUM_FLAG

      - name: Rename builds
        run: |
          cd build/app/outputs/flutter-apk
          mv app-armeabi-v7a-release.apk Omnigram-android-${{ inputs.version }}-armeabi-v7a.apk
          mv app-arm64-v8a-release.apk Omnigram-android-${{ inputs.version }}-arm64-v8a.apk
          mv app-x86_64-release.apk Omnigram-android-${{ inputs.version }}-x86_64.apk
          mv app-release.apk Omnigram-android-${{ inputs.version }}-universal.apk

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: android-artifacts
          path: app/build/app/outputs/flutter-apk/*.apk
          if-no-files-found: error
